# مستند فنی: ماژول انبارگردانی

## ۱. هدف

این سند، الزامات مربوط به بخش «انبارگردانی» را برای تیم فنی و برنامه‌نویسی مستند می‌کند تا بر اساس آن پیاده‌سازی انجام شود. این بخش، امتداد و مکمل بخش انبار موجود است و دو ستون جدید به آن اضافه می‌شود.

## ۲. ساختار کلی

بخش انبارگردانی همان ساختار بخش انبار فعلی را دارد، با این تفاوت که دو فیلد زیر به آن افزوده می‌شود:

- **کد محصول**
- **خالص موجودی**

علاوه بر این، سه وضعیت برای هر قطعه محصول تعریف می‌شود:

1. سفارش داده‌شده
2. منهدم‌شده
3. فروخته‌شده

## ۳. تعریف وضعیت‌ها

### ۳.۱. منهدم‌شده

منظور از «منهدم‌شده»، قطعاتی از یک محصول است که پس از ورود به انبار، معیوب تشخیص داده شده یا امکان فروش آن‌ها وجود نداشته است.

**مثال:** فرض کنید ۲۰ عدد از یک محصول (مثلاً انگشتر) خریداری و وارد سیستم می‌شود. اگر ۵ عدد از این ۲۰ عدد معیوب باشند یا به فروش نروند، این ۵ عدد باید با وضعیت «منهدم‌شده» ثبت شوند.

### ۳.۲. فروخته‌شده

قطعاتی از محصول که به فروش رسیده‌اند، در این وضعیت قرار می‌گیرند.

## ۴. نحوه ثبت انهدام و فروش (ارتباط با لیست محصولات)

- در بخش «لیست محصولات»، هنگام تعریف قطعات یک محصول، سیستم برای هر قطعه یک **کد** تولید می‌کند.
- در مقابل هر کد، دو عملیات باید در دسترس باشد:
  1. **حذف (Delete)** — عملیات فعلی سیستم
  2. **انهدام محصول** — عملیات جدیدی که باید اضافه شود

- کاربر باید بتواند با انتخاب کد یا کدهای مشخص (مثلاً کد قطعه شماره ۱۳ و شماره ۱۷ از یک محصول)، آن‌ها را با وضعیت «منهدم‌شده» علامت‌گذاری کند.
- به همین ترتیب، فروخته‌شدن هر قطعه نیز باید بر اساس کد آن قطعه در سیستم ثبت و ردیابی شود.

> نکته: عملیات «حذف» و «انهدام محصول» دو عملیات مجزا هستند و باید هر دو به‌صورت مستقل در لیست محصولات وجود داشته باشند.

## ۵. فیلدهای مورد نیاز در صفحه انبارگردانی

برای هر محصول، اطلاعات زیر باید در بخش انبارگردانی نمایش داده شود:

| فیلد | توضیح |
|---|---|
| کد محصول | کد یکتای هر قطعه |
| خالص موجودی | تعداد موجودی واقعی و قابل فروش |
| تعداد سفارش داده‌شده | تعداد قطعاتی که سفارش داده شده‌اند |
| تعداد منهدم‌شده | تعداد قطعاتی که خراب یا غیرقابل‌فروش تشخیص داده شده‌اند |
| تعداد فروخته‌شده | تعداد قطعاتی که فروخته شده‌اند |
| وزن کل موجودی | مجموع وزن تمام قطعات موجود از آن محصول |
| مجموع کدهای محصول | تعداد کل کدهای تعریف‌شده برای آن محصول |

**توضیح تکمیلی:** طبق تصمیم قبلی تیم، اطلاعات سفارش/انهدام/فروش نباید بخشی از «خالص موجودی» انبار محسوب شود؛ خالص موجودی باید فقط موجودی واقعی و قابل‌فروش را نشان دهد. اما تعداد کل قطعات (شامل منهدم‌شده و فروخته‌شده) نیز باید به‌صورت جداگانه و در کنار آن قابل مشاهده باشد.

## ۶. الزامات گزارش‌گیری

سیستم باید امکان فیلتر و گزارش‌گیری بر اساس معیارهای زیر را در بخش انبارگردانی فراهم کند:

1. **بیشترین فروش:** امکان مشاهده پرفروش‌ترین محصول در هر گروه (مثلاً در گروه «انگشتر»، کدام محصول بیشترین فروش را داشته است).
2. **بیشترین انهدام/خرابی:** امکان مشاهده محصولی که بیشترین میزان انهدام یا خرابی را داشته است.

**هدف کاربردی:** این گزارش باید به تصمیم‌گیری‌های تجاری کمک کند؛ برای مثال اگر محصولی از یک تأمین‌کننده مشخص بیشترین نرخ خرابی را داشته باشد، امکان شناسایی آن و توقف سفارش‌های بعدی از آن منبع فراهم شود.

## ۷. جمع‌بندی الزامات پیاده‌سازی

- افزودن دو فیلد جدید به انبار: کد محصول، خالص موجودی
- افزودن سه وضعیت به قطعات: سفارش داده‌شده، منهدم‌شده، فروخته‌شده
- افزودن عملیات «انهدام محصول» در کنار عملیات «حذف» در لیست محصولات، با قابلیت انتخاب بر اساس کد قطعه
- محاسبه و نمایش وزن کل موجودی و مجموع کدهای هر محصول
- افزودن گزارش «بیشترین فروش» و «بیشترین انهدام» به تفکیک گروه محصول

---
*در صورت وجود ابهام یا سوال در مورد هرکدام از بندهای بالا، لطفاً پیش از شروع پیاده‌سازی مطرح شود.*
